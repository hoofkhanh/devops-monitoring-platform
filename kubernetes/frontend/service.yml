apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: devops-monitoring-platform
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 5173
      protocol: TCP
